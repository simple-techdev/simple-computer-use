"""simple-computer-use — computer-use tools for any agent.

Turns the host machine's screens, mouse, and keyboard into a small set of
tools (screenshot / ground / click / type / scroll / drag / hotkey / open /
find-text) that any agent can drive through the `scu` CLI or the optional
MCP server.
"""

__version__ = "0.1.0"
